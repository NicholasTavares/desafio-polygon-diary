import React from 'react'

const Options = ({text}) => {

    //TODO: fazer função que chame na api usando 'text' como parametro

    return (
        <div className='header__options'>
            {text}
        </div>
    )
}

export default Options